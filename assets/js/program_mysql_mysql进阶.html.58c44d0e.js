"use strict";(self.webpackChunkxwblog=self.webpackChunkxwblog||[]).push([[7191],{66262:(i,t)=>{t.A=(i,t)=>{const l=i.__vccOpts||i;for(const[i,n]of t)l[i]=n;return l}},66347:(i,t,l)=>{l.r(t),l.d(t,{comp:()=>m,data:()=>D});var n=l(20641);const s=l.p+"assets/img/20250817154747.54646488.png",e=l.p+"assets/img/20250817154748.7767ee95.png",o=l.p+"assets/img/20250817162525.057fd1c1.png",r=l.p+"assets/img/20250817172121.c9e1a030.png",a=l.p+"assets/img/20250817172020.b41e7e57.png",d=l.p+"assets/img/20250817162828.899da848.png",g=l.p+"assets/img/1755423157601.9b466b51.png",p=l.p+"assets/img/1755422949483.e42bccf0.png",c=l.p+"assets/img/1751464621146.7f14c085.png",h=l.p+"assets/img/1751464602630.6150f9cf.png",u=l.p+"assets/img/1751464640606.2cd67714.png",_=l.p+"assets/img/1751791011316.76eeed06.png",E=l.p+"assets/img/1751790646266.a9316218.png",k=(0,n.Lk)("h1",{id:"mysql进阶",tabindex:"-1"},[(0,n.Lk)("a",{class:"header-anchor",href:"#mysql进阶"},[(0,n.Lk)("span",null,"Mysql进阶")])],-1),b=(0,n.Lk)("p",null,"索引与算法、事务与锁...",-1),A=(0,n.Fv)('<h1 id="索引与算法" tabindex="-1"><a class="header-anchor" href="#索引与算法"><span>索引与算法</span></a></h1><h2 id="_1-索引结构" tabindex="-1"><a class="header-anchor" href="#_1-索引结构"><span>1. 索引结构</span></a></h2><h3 id="_1-1-b树与b-树" tabindex="-1"><a class="header-anchor" href="#_1-1-b树与b-树"><span>1.1 B树与B+树</span></a></h3><p>提供一个网站 可以动态演示数据结构插入状态 <a href="https://www.cs.usfca.edu/~galles/visualization/Algorithms.html" target="_blank" rel="noopener noreferrer">数据结构可视化</a></p><p><strong>B树</strong>：</p><figure><img src="'+s+'" alt="1751464621146" tabindex="0" loading="lazy"><figcaption>1751464621146</figcaption></figure><p>5阶的B树，每一个节点最多存储4个key，对应5个指针。 一旦节点存储的key数量到达5，就会裂变，中间元素向上分裂。 在B树中，非叶子节点和叶子节点都会存放数据。结合叶子结点和非叶子结点才能构成完整的数据，每一个几点就是一个<strong>页</strong>（InnoDB 最小控制单位）</p><p><strong>B+树</strong></p><figure><img src="'+e+'" alt="1751464621146" tabindex="0" loading="lazy"><figcaption>1751464621146</figcaption></figure><p>在B树上进行改进：</p><ol><li>非叶子节点不存储数据，只存储索引 用来当做目录 加快检索能力。所有数据都可以在叶子节点找到，叶子节点形成一个<strong>双向链表</strong>。</li><li>在裂变时中间元素向上裂变 但是原节点仍包含中间元素。</li></ol><p><strong>为什么选择 B+Tree</strong>？</p><ul><li><strong>高扇出性：</strong> 非叶子节点不存储实际数据，只存储键值和指针，使得一个节点可以容纳非常多的指针（子节点）。这大大降低了树的高度（通常只有 3-4 层），意味着查找任何数据通常只需要 3-4 次磁盘 I/O（假设节点大小与磁盘页匹配）。</li><li><strong>稳定的查询效率：</strong> 任何查询都需要从根节点遍历到叶子节点，路径长度相同（树的高度），查询效率稳定。</li><li><strong>卓越的范围查询性能：</strong> 叶子节点有序且通过链表连接，使得范围查询（WHERE col BETWEEN a AND b）异常高效，只需定位到起始点然后顺序遍历链表即可。</li><li><strong>全表扫描优化：</strong> 遍历链表就能按索引顺序获取所有数据（对于聚簇索引就是所有行数据），比遍历无序的数据页快得多。</li><li><strong>更适合磁盘存储：</strong> 节点大小通常设计为磁盘页大小（如 16KB）的倍数，能最大化利用磁盘 I/O 的块读取特性，减少随机 I/O。</li></ul><p>那么正常一棵B+树可以存<strong>储多少数据</strong>呢？我们来算一下</p><p>一个页的大小是<strong>16K</strong></p><p>假设： 一行数据大小为1k，一页中可以存储16行这样的数据。InnoDB的指针占用6 个字节的空间，主键即使为bigint，占用字节数为8 。</p><p><strong>高度为2</strong>: n * 8+ (n+1) * 6 = 16 * 1024 ，算出n约为 1170 1171 * 16 = 18736</p><p><strong>高度为3</strong>： 1171* 1171 * 16 = 21939856 <strong>大约两千万条数据</strong></p><h2 id="_2-索引的分类" tabindex="-1"><a class="header-anchor" href="#_2-索引的分类"><span>2. 索引的分类</span></a></h2><p>索引按照<strong>物理层</strong>可分为<strong>聚簇索引</strong>、<strong>二级索引（非聚簇索引）</strong></p><ol><li><p><strong>聚簇索引（Clustered Index）：</strong></p><ol><li>表数据行的物理存储顺序与索引的逻辑顺序相同。一个表<strong>有且只有一个</strong>聚簇索引。</li><li>InnoDB 的实现： <ul><li>如果定义了主键（PRIMARY KEY），主键索引就是聚簇索引。</li><li>如果没有显式定义主键，则选择第一个具有唯一非空约束（UNIQUE NOT NULL）的列作为聚簇索引。</li><li>如果以上都没有，InnoDB 会隐式创建一个隐藏的 RowID 作为聚簇索引。</li></ul></li><li><strong>特点</strong>： <ul><li>叶子节点存储完整数据行。按主键查询极快（一次索引查找即得数据）。</li><li>范围查询（按主键范围）高效。</li><li>插入速度依赖于插入顺序（按主键顺序插入最快，避免页分裂）。</li><li>更新主键代价高（会导致行移动）。</li></ul></li></ol></li><li><p><strong>二级索引（Secondary Index / Non-Clustered Index / 辅助索引）：</strong></p><ul><li>除了聚簇索引以外的所有索引都是二级索引。</li><li>InnoDB 的实现： 叶子节点存储该索引的<strong>键值</strong>和对应行的<strong>主键值</strong>。</li><li><strong>查询过程（回表）：</strong> 使用二级索引查询时，先在二级索引树中找到目标键值和对应的主键值，然后拿着这个主键值回到聚簇索引树中查找完整的行数据。这个“回表”操作会产生额外的 I/O。</li><li><strong>特点：</strong><ul><li>叶子节点不包含完整数据，需要回表。</li><li>查询效率通常低于直接使用聚簇索引（因为有回表开销）。</li><li>覆盖索引可以避免回表（见下文）。</li></ul></li></ul></li></ol><figure><img src="'+o+'" alt="1751464621146" tabindex="0" loading="lazy"><figcaption>1751464621146</figcaption></figure><p>按照<strong>字段</strong>来分索引的话 又可以分为</p><ul><li><p><strong>主键索引（PRIMARY KEY）：</strong></p><ul><li>特殊的唯一索引。每个表只能有一个。</li><li>强制要求索引键值唯一且非空（NOT NULL）。</li><li>在 InnoDB 中，它就是聚簇索引。</li><li>是逻辑行（Row）的唯一标识。</li></ul></li><li><p><strong>唯一索引（UNIQUE INDEX）：</strong></p><ul><li>强制要求索引键值<strong>唯一</strong>。允许有 NULL 值（但通常只能有一个 NULL，具体行为取决于数据库设置和版本）。</li><li>用于保证数据唯一性约束。</li><li>可以是聚簇索引（如果它是主键）或二级索引。</li></ul></li><li><p><strong>普通索引（INDEX / KEY）：</strong></p><ul><li>最基本的索引类型，没有任何唯一性约束。</li><li>纯粹为了加速查询。</li><li>只能是二级索引。</li></ul></li><li><p><strong>Full-Text Index（全文索引）：</strong></p><ul><li>结构： 专门为文本内容搜索（MATCH ... AGAINST）设计。早期 MyISAM 支持，现在 InnoDB 也支持。底层通常使用倒排索引（Inverted Index），记录单词到文档（行）的映射。</li><li>特点： 支持自然语言搜索、布尔搜索、相关性排序等。</li></ul></li><li><p><strong>R-Tree（空间索引）：</strong></p><ul><li>结构： 用于存储和查询空间数据（如地理位置 GEOMETRY）。</li><li>特点： 支持快速查找包含某个点、与某个形状相交或在某个区域内的对象。</li><li>引擎： MyISAM 和 InnoDB（MySQL 5.7+）支持。</li></ul></li><li><p><strong>联合索引（Composite Index / Compound Index）：</strong></p><ul><li>基于<strong>多个列</strong>组合创建的索引。</li><li>索引键值由多个列的值按定义顺序连接组成（如 <code>(col1, col2, col3)</code>）。</li><li><strong>最左前缀匹配原则：</strong> 这是联合索引查询有效性的核心规则。查询条件必须<strong>包含索引定义中最左边的列</strong>（或连续的最左前列组合），索引才能被有效使用。 <ul><li>索引 <code>(A, B, C)</code> 能有效用于 <code>WHERE A=...</code>, <code>WHERE A=... AND B=...</code>, <code>WHERE A=... AND B=... AND C=...</code>, <code>WHERE A=... AND C=...</code>（C 条件可能无法完全利用，但 A 能定位范围）。</li><li>索引 <code>(A, B, C)</code> <strong>不能</strong>有效用于 <code>WHERE B=...</code>, <code>WHERE C=...</code>, <code>WHERE B=... AND C=...</code>（因为缺少最左列 A）。</li></ul></li><li>联合索引可以避免为多个单列索引创建多个索引结构，有时还能实现覆盖索引。</li></ul><figure><img src="'+r+'" alt="1751464621146" tabindex="0" loading="lazy"><figcaption>1751464621146</figcaption></figure></li><li><p><strong>前缀索引（Prefix Index）：</strong></p><ul><li>对于很长的字符列（如 VARCHAR(255)），可以为该列的前 N 个字符创建索引，而不是整个列。</li><li>目的： 节省索引存储空间和提高索引效率（比较更短的字符串更快）。</li><li>缺点**：** <ul><li>降低了索引的选择性（区分度），可能导致查询需要扫描更多行。</li><li>无法用于 <code>ORDER BY</code> 或 <code>GROUP BY</code>（除非显式指定前缀长度 <code>ORDER BY col_name(20)</code>，但这不常见）。</li><li>无法用于覆盖索引（除非 SELECT 的列刚好也是前缀）。</li></ul></li><li>创建： <code>CREATE INDEX idx_name ON table_name (column_name(N));</code></li></ul><figure><img src="'+a+'" alt="1751464621146" tabindex="0" loading="lazy"><figcaption>1751464621146</figcaption></figure></li><li><p><strong>自适应哈希索引（Adaptive Hash Index - AHI）：</strong></p><ul><li><strong>InnoDB 特有特性。</strong></li><li>数据库<strong>内部自动创建和维护</strong>的基于内存的哈希索引。</li><li>它<strong>监控</strong>对 B+Tree 索引页的频繁访问模式。如果一个索引页被非常频繁地以相同模式（通常是等值查询）访问，InnoDB 会在内存中为该页构建一个哈希索引，以加速后续对该页的访问。</li><li>用户不可控： DBA 无法显式创建或删除 AHI。可以通过参数 <code>innodb_adaptive_hash_index</code> 开启或关闭。</li><li>优点：对特定热点数据的等值查询有显著加速效果。</li><li>缺点： 占用内存；在高并发写场景下可能成为瓶颈（维护哈希结构的锁竞争）。</li></ul></li></ul><h2 id="_3-索引的特征" tabindex="-1"><a class="header-anchor" href="#_3-索引的特征"><span>3. 索引的特征</span></a></h2><h3 id="_3-1-回表" tabindex="-1"><a class="header-anchor" href="#_3-1-回表"><span>3.1 回表</span></a></h3><p>什么回表呢？</p><figure><img src="'+d+'" alt="1751464621146" tabindex="0" loading="lazy"><figcaption>1751464621146</figcaption></figure><p><strong>回表原因</strong>：因为在语句查询时如果使用的是非聚簇索引，那么其叶子结点的数据是不全的 就需要从非聚簇索引的叶子结点拿到聚簇索引的索引值（聚簇索引拥有全数据）然后去聚簇索引再次进行查询 拿到该条数据的全部数据。</p><p><strong>执行流程</strong>：</p><ol><li><p>由于是根据name字段进行查询，所以先根据name=&#39;Arm&#39;到name字段的二级索引中进行匹配查找。但是在二级索引中只能查找到 Arm 对应的主键值 10。</p></li><li><p>由于查询返回的数据是*，所以此时，还需要根据主键值10，到聚集索引中查找10对应的记录，最 终找到10对应的行row。</p></li><li><p>最终拿到这一行的数据，直接返回即可。</p></li></ol><blockquote><p>回表查询： 这种先到二级索引中查找数据，找到主键值，然后再到聚集索引中根据主键值，获取</p><p>数据的方式，就称之为回表查询。</p></blockquote><h3 id="_3-2-索引覆盖" tabindex="-1"><a class="header-anchor" href="#_3-2-索引覆盖"><span>3.2 索引覆盖</span></a></h3><p>覆盖索引：</p><ul><li>指一个查询<strong>只需要访问索引本身</strong>就能获取到所需的全部数据列，<strong>无需回表</strong>（对于二级索引）或访问数据页（对于聚簇索引）。</li><li><strong>如何实现：</strong> SELECT 语句中查询的列都包含在某个索引（单列或联合索引）的键值中（对于二级索引，主键值总是包含的）。</li><li><strong>巨大优势：</strong> 极大地提升查询性能，因为只需要扫描索引树（通常更小、更紧凑），避免了昂贵的回表或数据页访问操作。</li><li>例如：索引 <code>(user_id, name)</code> 对于查询 <code>SELECT name FROM users WHERE user_id = 100;</code> 就是覆盖索引。</li></ul><h3 id="_3-3-索引下推" tabindex="-1"><a class="header-anchor" href="#_3-3-索引下推"><span>3.3 索引下推</span></a></h3><p>索引下推：</p><ul><li><strong>解决的问题：</strong> 在二级索引查询中，即使索引未能完全覆盖 WHERE 条件（部分条件涉及索引未包含的列），传统的做法是先在二级索引中找到所有匹配索引键值的行（得到主键），然后回表取出完整行数据，最后在 Server 层应用剩余的 WHERE 条件进行过滤。如果二级索引找到的行很多，但大部分都被剩余条件过滤掉了，回表操作就是巨大的浪费。</li><li><strong>ICP 原理：</strong> 将 WHERE 子句中<strong>涉及索引列但无法在索引中完全评估</strong>的条件（比如联合索引 <code>(A, B)</code> 中的 <code>B LIKE &#39;%abc&#39;</code> 或 <code>B &gt; 10</code>，或者对索引列应用了函数但优化器能下推部分计算），<strong>“下推”到存储引擎层去执行</strong>。存储引擎在遍历二级索引时，会利用这些下推的条件<strong>直接在索引层面进行初步过滤</strong>，只将满足所有（可下推）索引条件的行对应的主键值回表去取数据。</li><li><strong>巨大优势：</strong> 显著减少了不必要的回表次数，提升了查询性能，特别是对于联合索引和包含 <code>LIKE</code>、范围查询等条件的查询。使用 <code>EXPLAIN</code> 查看执行计划，如果 <code>Extra</code> 列出现 <code>Using index condition</code>，就表示使用了 ICP。</li></ul><p>举例说明：</p><p>假设表<code>user</code>有联合索引<code>(a, b)</code>，执行以下查询：</p><div class="language-sql line-numbers-mode" data-highlighter="shiki" data-ext="sql" data-title="sql" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code><span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">SELECT</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> * </span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">FROM</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> user </span></span>\n<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">WHERE</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> a </span><span style="--shiki-light:#383A42;--shiki-dark:#56B6C2;">=</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> &#39;1&#39;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> </span></span>\n<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">AND</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> b </span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">LIKE</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> &#39;%dudu%&#39;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> </span></span>\n<span class="line"><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">AND</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> c </span><span style="--shiki-light:#A626A4;--shiki-dark:#C678DD;">LIKE</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> &#39;%xiaowu%&#39;</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">;</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><p><strong>传统方式</strong>：</p></li><li><ul><li>存储引擎利用<code>a</code>定位索引条目，获取所有<code>a=&#39;1&#39;</code>的主键。</li><li>回表查询所有对应数据行后，服务器层再过滤<code>b</code>和<code>c</code>条件。</li></ul></li><li><p><strong>索引下推</strong>：</p></li><li><ul><li>存储引擎在扫描索引时，先根据<code>a</code> 将数据查询出来，然后直接应用<code>b LIKE &#39;%dudu%&#39;</code>条件进行过滤</li><li>仅符合条件的索引条目对应的主键需要回表，返回给sever层，sever层再过滤<code>c</code>条件，完成数据筛选。</li></ul></li><li><p><strong>效果</strong>：减少回表次数，避免读取无效数据。</p></li><li><p><strong>为什么b的模糊查询能被下推？</strong></p></li><li><ul><li>虽然<code>b LIKE &#39;%dudu%&#39;</code>是模糊匹配，但只要b字段在索引中，存储引擎就能直接通过索引内容进行过滤（即使无法利用索引的有序性）</li></ul></li></ul><h3 id="_3-4-最左匹配原则" tabindex="-1"><a class="header-anchor" href="#_3-4-最左匹配原则"><span>3.4 最左匹配原则</span></a></h3><p>在联合索引中，查询条件必须包含索引的最左列，且遵循从左到右的连续顺序。若跳过左列或顺序不连续，索引将部分失效或完全失效。这是怎么回事？</p><p><strong>联合索引存储结构</strong>：联合索引按列顺序构建 B+ 树。例如索引 <code>(a, b, c)</code>，数据先按 <code>a</code> 排序，<code>a</code> 相同则按 <code>b</code> 排序，<code>b</code> 相同再按 <code>c</code> 排序。所以说如果查询时最左边第一个索引都没有命中话 则无法命中索引</p><p>查询逻辑：只有从左开始连续匹配，才能利用索引的有序性快速定位数据。跳过左列会导致无法利用索引结构。</p><p>举个<strong>栗子</strong>吧：</p><p>假设表 user 有联合索引 (a, b, c)，不同查询条件的索引使用情况如下：</p><ol><li><p><strong>有效匹配索引</strong></p><ul><li><p>查询条件：WHERE a = 1 AND b = 2</p><p>结果：索引生效，同时使用 a 和 b 两列。</p></li><li><p>查询条件：WHERE a = 1 AND c = 3</p><p>结果：仅 a 生效，c 因跳过 b 无法使用索引。</p></li></ul></li><li><p><strong>无效匹配索引</strong></p><ul><li><p>查询条件：WHERE b = 2 AND c = 3</p><p>结果：完全无法使用索引，触发全表扫描。</p></li></ul></li><li><p><strong>范围查询的影响</strong></p><ul><li><p>查询条件：WHERE a = 1 AND b &gt; 2 AND c = 3</p><p>结果：a 和 b 生效，c 因范围查询 b &gt; 2 而失效。</p></li></ul></li></ol><h2 id="_4-索引的失效场景" tabindex="-1"><a class="header-anchor" href="#_4-索引的失效场景"><span>4. 索引的失效场景</span></a></h2><p>索引会在什么情况下失效呢？</p><ol><li><p><strong>违反最左前缀原则：</strong> 联合索引未使用最左列。</p><p><strong>解决方案</strong>：</p><ul><li><p>调整查询条件顺序，确保联合索引最左列在条件中。</p></li><li><p>重构索引结构（如新增 <code>(b,c)</code> 索引）。</p></li></ul></li><li><p><strong>对索引列进行计算或函数操作：</strong> <code>WHERE YEAR(date_col) = 2023</code> (除非有函数索引) vs <code>WHERE date_col BETWEEN &#39;2023-01-01&#39; AND &#39;2023-12-31&#39;</code>。</p><p>原因：索引存储原始值，计算或函数破坏索引的有序性，需全表扫描</p><p>解决方案：</p><ul><li>将运算移至等号右侧：WHERE age &gt; 20-1。</li><li>使用持久化计算字段（如生成列）代替动态计算。</li></ul></li><li><p><strong>对索引列使用隐式类型转换：</strong><code> WHERE phone = 13800138000</code>（phone为varchar类型）。</p><p>原因：数据库需隐式转换类型，等效于对索引字段调用函数，导致失效[21] [25]。</p><p>解决方案：</p><ul><li>统一数据类型：WHERE phone = &#39;13800138000&#39;。</li></ul></li><li><p><strong>使用 != 或 &lt;&gt;：</strong> 通常导致全表扫描（除非索引覆盖且优化器认为扫描索引比全表快）。</p><p>解决方案：</p><ul><li>使用是&gt;= 或者&lt;=则不会失效</li></ul></li><li><p><strong>使用 LIKE 以通配符 % 开头：</strong> <code>WHERE name LIKE &#39;%abc&#39;</code>。<code>&#39;abc%&#39;</code> 通常可以利用索引。</p><p>原因：B+树索引基于前缀匹配，无法定位不确定前缀的字符串</p><p>解决方案：</p><ul><li>使用后置模糊匹配<code>abc%</code></li></ul></li><li><p><strong>OR 条件未覆盖所有相关列：</strong> 如果 OR 连接的条件没有都覆盖索引，可能导致不使用索引。<code>WHERE a=1 OR b=2</code>，且 b 列无索引。</p><p>原因：OR条件若涉及非索引列，优化器无法有效合并结果，转为全表扫描</p><p>解决方案：</p><ul><li>拆分为UNION查询：<code>SELECT * FROM t WHERE a=1 UNION SELECT * FROM t WHERE b=2</code>。</li><li>为所有OR涉及的列单独建立索引。</li></ul></li><li><p><strong>索引列存在 NULL 值：</strong> 如果列允许 NULL，<code>WHERE col IS NULL</code> 可能（但不一定）利用索引，取决于优化器选择。<code>WHERE col IS NOT NULL</code> 通常效率较低。</p></li><li><p><strong>数据倾斜严重：</strong> 当某个值占比过高时，优化器可能认为全表扫描比索引扫描更快。</p></li></ol><h2 id="_5-索引的设计与优化" tabindex="-1"><a class="header-anchor" href="#_5-索引的设计与优化"><span>5. 索引的设计与优化</span></a></h2><h3 id="_5-1-索引的创建" tabindex="-1"><a class="header-anchor" href="#_5-1-索引的创建"><span>5.1 索引的创建</span></a></h3><ol><li><p><strong>为查询创建索引：</strong> 索引不是为表创建的，而是为<strong>频繁执行且性能关键的查询</strong>创建的。分析慢查询日志（Slow Query Log）是识别需要索引的查询的关键。</p></li><li><p><strong>选择合适的列：</strong></p><ul><li>针对于数据量较大，且查询比较频繁的表建立索引。</li><li>查询条件（where）、排序（order by）、分组（group by）<code>WHERE</code>、<code>JOIN ON</code>、<code>ORDER BY</code>、<code>GROUP BY</code> 子句中频繁出现的列。</li><li>考虑创建<strong>联合索引</strong>代替多个单列索引，并<strong>严格遵守最左前缀原则</strong>设计索引列顺序（将最常用于等值过滤和高选择性的列放左边；范围查询列放右边）。</li></ul></li><li><p>如果索引列不能存储NULL值，请在创建表时使用NOT NULL约束它。当优化器知道每列是否包含 NULL值时，它可以更好地确定哪个索引最有效地用于查询。</p></li><li><p>要控制索引的数量，<strong>索引并不是多多益善</strong>，索引越多，维护索引结构的代价也就越大，会影响增 删改的效率。</p></li></ol><h3 id="_5-2-sql优化" tabindex="-1"><a class="header-anchor" href="#_5-2-sql优化"><span>5.2 SQL优化</span></a></h3><p><strong>批量插入数据</strong></p><ul><li><p>如果一次性需要插入大批量数据(比如: 几百万的记录)，使用insert语句插入性能较低，此时可以使 用MySQL数据库提供的load指令进行插入。</p></li><li><p><strong>页分裂</strong> 页是数据库的最小控制单位 在数据插入时如果数据是乱序的 从中间插入数据时如果页大小不够了则会新开一个页将相邻数据页50%分隔开 和该数据组合进入新的数据页 然后数据页的双向链表指向该数据页</p></li></ul><figure><img src="'+g+'" alt="1755423157601" tabindex="0" loading="lazy"><figcaption>1755423157601</figcaption></figure><ul><li><strong>页合并</strong> 当一个页中的数据删除了50%（可配置 默认50%）时会查找相邻的页的数据 如果有能够融合成一页的就会进行合并</li></ul><figure><img src="'+p+'" alt="1755422949483" tabindex="0" loading="lazy"><figcaption>1755422949483</figcaption></figure><h1 id="事务与锁" tabindex="-1"><a class="header-anchor" href="#事务与锁"><span>事务与锁</span></a></h1><h2 id="_1-锁的分类" tabindex="-1"><a class="header-anchor" href="#_1-锁的分类"><span>1. 锁的分类</span></a></h2><p><strong>目的：</strong> 保证<strong>数据一致性</strong>（Consistency）和<strong>事务隔离性</strong>（Isolation）。当多个事务同时访问（读取或修改）相同数据时，锁机制防止出现脏读、不可重复读、幻读等问题，确保最终结果是正确的。</p><p>按照按锁的<strong>模式（访问级别）</strong> 划分（最核心）可分为： <strong>共享锁（Shared Lock, S Lock）<strong>和</strong>排他锁（Exclusive Lock, X Lock）</strong></p><p>按锁的<strong>粒度（锁定范围）</strong> 划分 可分为 ：<strong>表级锁（Table-Level Lock）</strong>、<strong>行级锁（Row-Level Lock）</strong>、<strong>页级锁（Page-Level Lock）</strong>、<strong>全局锁（Global Lock）</strong></p><h3 id="_1-1-共享锁和排他锁" tabindex="-1"><a class="header-anchor" href="#_1-1-共享锁和排他锁"><span>1.1 共享锁和排他锁</span></a></h3><ul><li><strong>共享锁（Shared Lock, S Lock）：</strong><ul><li>目的： 用于<strong>读取</strong>操作。</li><li><strong>兼容性：</strong> 多个事务可以<strong>同时</strong>持有同一数据资源上的 S 锁（读读兼容）。</li><li><strong>排斥性：</strong> 如果某个事务持有 S 锁，其他事务<strong>不能</strong>获得该资源上的 X 锁（读写不兼容）。</li><li>加锁语句： 默认的 <code>SELECT</code> 语句<strong>不加</strong> S 锁（使用 MVCC）。显式加 S 锁使用 <code>SELECT ... LOCK IN SHARE MODE</code> (旧语法) 或 <code>SELECT ... FOR SHARE</code> (MySQL 8.0+ 推荐语法)。</li></ul></li><li><strong>排他锁（Exclusive Lock, X Lock）：</strong><ul><li>目的： 用于<strong>写入</strong>操作（INSERT, UPDATE, DELETE）。</li><li><strong>兼容性：</strong> <strong>最不兼容</strong>的锁。如果一个事务持有 X 锁，其他事务<strong>不能</strong>获得该资源上的 <strong>任何</strong> 锁（S 或 X）（写写、读写都不兼容）。</li><li>加锁语句： <code>INSERT</code>, <code>UPDATE</code>, <code>DELETE</code> 语句在操作数据行时会<strong>自动</strong>加 X 锁。显式加 X 锁使用 <code>SELECT ... FOR UPDATE</code>。</li></ul></li></ul><h3 id="_1-2-全局锁" tabindex="-1"><a class="header-anchor" href="#_1-2-全局锁"><span>1.2 全局锁</span></a></h3><ul><li><code>FLUSH TABLES WITH READ LOCK (FTWRL)</code>： 加全局读锁。使整个数据库实例处于只读状态。用于备份等场景。阻塞所有写操作和大部分 DDL。非常重量级。</li><li><code>set global read_only=1</code>： 设置只读模式。比 FTWRL 轻量，但可能被具有 SUPER 权限的用户绕过。</li></ul><p>全局锁是针对整个数据库而言的，从名称我们也能了解到大概意思，当我们对数据库加了全局读锁之后，那么其他任何对请求都不能对数据库再加写锁了，当我们对数据加了写锁后，那么后续对数据的加读锁和写锁的请求都会被阻塞。</p><p>全局锁只有在进行<strong>主从备份数据</strong>、或者<strong>导入导出数据</strong>的时候才会对全局进行加锁。</p><p>为什么需要全局锁?</p><p>因为我们在做备份数据或者导入导出数据操作的时候，如果这个系统期间还可以进行数据的增、删、改，那么就会有一个数据“时点性”问题，会导致数据不一致。</p><p>比如一个事务要修改学生表和学生选修课表。由于我们之前已经备份过学生表了，这时候插入两张表的数据，我们只能备份到选修课表的新数据，对于学生表的老数据就看不到了，造成不一致。</p><p>这个例子按照正常思维会很疑惑，我们可以改隔离级别，做到可重复读的级别，这样别的事务就不会影响到我们读取了。是这样的，但是很多存储引擎是没有mvcc的，为了避免数据插入，只能上表锁，全局锁。</p><h3 id="_1-3-元数据锁-dml" tabindex="-1"><a class="header-anchor" href="#_1-3-元数据锁-dml"><span>1.3 元数据锁（DML）</span></a></h3><p>MDL （meta data lock）其实也可以算表锁，之所以拆开聊，是因为它是Server层的锁，在MySQL5.5版本引入了MDL。</p><p>当对一个表做<strong>增删改查操作</strong>的时候，加MDL读锁；</p><p>当要对表做<strong>结构变更操作DDL</strong>的时候，加MDL写锁。</p><p>MDL不需要我们显式使用，在访问一个表的时候会被自动加上。MDL的作用是，保证读写的正确性。如果一个查询正在遍历一个表中的数据，而执行期间另一个线程对这个表结构做了变更，删了一列，那么查询线程拿到的结果跟表结构对不上，肯定不行</p><p>读锁之间不互斥，因此可以有多个线程同时对一张表增删改查</p><p>读写锁之间、写锁之间是互斥的，用来保证变更表结构操作的安全性。</p><p>举个栗子：</p><div class="language-sql line-numbers-mode" data-highlighter="shiki" data-ext="sql" data-title="sql" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code><span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">#</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">1</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.开启事务，查询 对MDL加读锁</span></span>\n<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">\t\t\t\t\t\t #</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">2</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.修改表名，尝试加MDL写锁，排队</span></span>\n<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">\t\t\t\t\t\t\t\t\t\t\t\t\t#</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">3</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.尝试获取MDL读锁，但是依然排队</span></span>\n<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">#</span><span style="--shiki-light:#986801;--shiki-dark:#D19A66;">4</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">.提交事务，释放MDL读锁</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="_1-4-表锁" tabindex="-1"><a class="header-anchor" href="#_1-4-表锁"><span>1.4 表锁</span></a></h3><ul><li>锁定范围： 整个表。</li><li><strong>优点：</strong> 实现简单，加锁/解锁开销小。</li><li><strong>缺点：</strong> 并发度最低。一个事务锁表后，其他访问该表的事务都会被阻塞。</li><li>类型： <ul><li><strong>表级共享锁（S）：</strong> <code>LOCK TABLES table_name READ;</code> 手动加。允许其他读，阻塞写。</li><li><strong>表级排他锁（X）：</strong> <code>LOCK TABLES table_name WRITE;</code> 手动加。阻塞其他所有读写。</li><li><strong>意向锁（Intention Lock）：</strong> <strong>InnoDB 特有且关键</strong>的<strong>表级锁</strong>，用于<strong>协调</strong>行锁与表锁的关系。它<strong>表明</strong>一个事务<strong>打算</strong>在表中的某些行上加什么类型的锁（行级 S 或 X 锁）。 <ul><li><strong>意向共享锁（Intention Shared Lock, IS）：</strong> 事务<strong>打算</strong>给表中的某些行加 S 锁。<code>SELECT ... FOR SHARE</code> 会先加 IS 锁。</li><li><strong>意向排他锁（Intention Exclusive Lock, IX）：</strong> 事务<strong>打算</strong>给表中的某些行加 X 锁。<code>SELECT ... FOR UPDATE</code>, <code>INSERT</code>, <code>UPDATE</code>, <code>DELETE</code> 会先加 IX 锁。</li></ul></li><li><strong>意向锁的兼容性：</strong><ul><li>IS 与 IS 兼容</li><li>IS 与 IX 兼容</li><li>IX 与 IX 兼容</li><li><strong>表级 S 锁</strong>： 与 IS 兼容，<strong>与 IX 不兼容</strong>。</li><li><strong>表级 X 锁</strong>： <strong>与 IS 和 IX 都不兼容</strong>。</li></ul></li></ul></li></ul><table><thead><tr><th>锁类型 \\ 兼容性</th><th>共享锁 (S)</th><th>排他锁 (X)</th><th>意向共享锁 (IS)</th><th>意向排他锁 (IX)</th></tr></thead><tbody><tr><td><strong>共享锁 (S)</strong></td><td>兼容</td><td>冲突</td><td>兼容</td><td>冲突</td></tr><tr><td><strong>排他锁 (X)</strong></td><td>冲突</td><td>冲突</td><td>冲突</td><td>冲突</td></tr><tr><td><strong>意向共享锁 (IS)</strong></td><td>兼容</td><td>冲突</td><td>兼容</td><td>兼容</td></tr><tr><td><strong>意向排他锁 (IX)</strong></td><td>冲突</td><td>冲突</td><td>兼容</td><td>兼容</td></tr></tbody></table><h4 id="_1-4-1-意向锁" tabindex="-1"><a class="header-anchor" href="#_1-4-1-意向锁"><span>1.4.1 意向锁</span></a></h4><p>意向锁理解：假设事务 A 持有表 T 中某行的 X 锁（意味着它持有 T 的 IX 锁）。事务 B 想给整个表 T 加 S 锁。如果没有意向锁，B 需要扫描所有行检查是否有行锁冲突，效率极低。有了意向锁，B 发现 T 上已有 IX 锁（表示有事务持有或将要持有行级 X 锁），而 IX 与表级 S 锁不兼容，因此 B 会立即阻塞，无需扫描行。</p><p>总结就是 当一个事务要对表中的某一行加行级锁时，会先在表上加对应的意向锁。其他事务想对表<strong>加表级锁</strong>时，只要检查到有意向锁，就知道表中已经有行被加锁了，就不用全表扫描来判断啦</p><h4 id="_1-4-2-自增锁" tabindex="-1"><a class="header-anchor" href="#_1-4-2-自增锁"><span>1.4.2 自增锁</span></a></h4><p>当你创建一张表给主键列设置了自增id INT NOT NULL AUTO_INCREMENT,那么插入的时候我们就不需要指定id了，它会自动添加自增的值。实现并发安全获取自增id有两种方式，和系统变量innodb_autoinc_lock_mode有关。 首先insert大致上可以分成三类：</p><p>innodb_autoinc_lock_mode` 变量一共有三个不同的取值：</p><p><strong>0</strong>：这个表示 traditional，在这种模式下，都是在插入 SQL 语句开始的时候，获取到一个表级的 AUTO-INC 锁（虽然是表级锁，但只是为了对不同行的插入保证线程安全，和IS,IX,S,X这些锁是不冲突的），然后当插入 SQL 执行完毕之后，再释放掉这把锁，这样做的好处是可以确保在批量插入的时候，自增主键是连续的。它和传统锁不同的是，传统锁在事务结束后才释放，而它是在insert语句执行后立马就释放锁。</p><p><strong>2</strong>: 这个表示 interleaved，在这种模式下，不存在 AUTO-INC 锁而是有一个轻量级的锁， 在为插入语句生成 AUTO_INCREMENT 修饰的列的值时获取一下这个轻量级锁， 然后获取到自增值。两个锁的区别在于，会不会和表锁相冲突。批量插入的时候，就有可能出现主键虽然自增，但是不连续的问题。 从上面的介绍中小伙伴们可以看到，实际上第三种，也就是 innodb_autoinc_lock_mode 取值为 2 的情况下，并发效率是最强的，那么我们是不是就应该设置 innodb_autoinc_lock_mode=2 呢？这得看情况。</p><p>之前介绍过MySQL binlog 日志文件的三种格式：</p><ul><li>row：binlog 中记录的是具体的值而不是原始的 SQL，举一个简单例子，假设表中有一个字段是 UUID，用户执行的 SQL 是 insert into user(username,uuid) values(&#39;javaboy&#39;,uuid())，那么最终记录到 binlog 中的 SQL 是 insert into user(username,uuid) values(&#39;javaboy&#39;,‘0212cfa0-de06-11ed-a026-0242ac110004’)。</li><li>statement：binlog 中记录的就是原始的 SQL 了，以 row 中的为例，最终 binlog 中记录的就是 insert into user(username,uuid) values(&#39;javaboy&#39;,uuid())。</li><li>mixed：在这种模式下，MySQL 会根据具体的 SQL 语句来决定日志的形式，也就是在 statement 和 row 之间选择一种。</li></ul><p>对于这三种不同的模式，很明显，在主从复制的时候，statement 模式可能会导致主从数据不一致，所以现在</p><p>MySQL 默认的 binlog 格式都是 row。 因为statement主从用的是原始语句，如果 innodb_autoinc_lock_mode 的值为 2，可能会导致不同事务中插入语句获取到的值是不同的。</p><p>我们可以查看下自己数据库的值是多少。SHOW VARIABLES LIKE &#39;innodb_autoinc_lock_mode&#39;;</p><h3 id="_1-5-行锁" tabindex="-1"><a class="header-anchor" href="#_1-5-行锁"><span>1.5 行锁</span></a></h3><p>行级锁定最大的特点就是锁定对象的颗粒度很小，由于锁定颗粒度很小，所以发生锁定资源争用的概率也最小，能够给予应用程序尽可能大的并发处理能力而提高一些需要高并发应用系统的整体性能。</p><p>虽然能够在并发处理能力上面有较大的优势，但是行级锁定也因此带来了不少弊端。</p><p>由于锁定资源的颗粒度很小，所以每次获取锁和释放锁需要做的事情也更多，带来的消耗自然也就更大了。此外，行级锁定也最容易发生死锁</p><p><strong>行级锁，是加在索引上的</strong>，如果没有命中索引，会锁整个表吗？这个其实是个误区，没有命中索引，有可能会锁表，但是这个锁表和表锁没有任何关系，只是没命中索引就会在聚簇索引一条条找过去，查询经过的每一条记录都会被加锁，因此感觉上像是锁了表。</p><p>行级锁也分为共享锁S和排他锁X。插入语句加插入意向锁，删改语句加排他锁，查询语句默认不加锁。可以通过。<code>SELECT ... FOR UPDATE;SELECT ... LOCK IN SHARE MODE</code>;来对查询语句加X锁或者S锁。</p><p>行锁又分为：<strong>记录锁（Record Lock）</strong>、<strong>间隙锁（Gap Lock）</strong>、<strong>临键锁（Next-Key Lock）</strong>、<strong>插入意向锁（Insert Intention Lock）</strong></p><h4 id="_1-5-1-锁的结构" tabindex="-1"><a class="header-anchor" href="#_1-5-1-锁的结构"><span>1.5.1 锁的结构</span></a></h4><p>假设事务T1在表users的索引idx_age上对页号5的行加记录锁（X锁），其内存结构如下：</p><div class="language-txt line-numbers-mode" data-highlighter="shiki" data-ext="txt" data-title="txt" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code><span class="line"><span>type_mode   = 0x80000003 (高24位:LOCK_ORDINARY | 中4位:LOCK_REC | 低4位:LOCK_X)</span></span>\n<span class="line"><span>trx         = 0x7f8a3c00 (事务T1的地址)</span></span>\n<span class="line"><span>index       = 0x7f8a4d00 (idx_age索引的地址)</span></span>\n<span class="line"><span>hash_key    = {space_id=128, page_no=5, n_bits=64}</span></span>\n<span class="line"><span>lock_bitmap = 0x00000001 (第1行被锁定)</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>InnoDB的锁结构通过<code>type_mode</code>字段（32位无符号整型）集中管理锁模式和类型，其位分配规则如下：</p><div class="language-plain line-numbers-mode" data-highlighter="shiki" data-ext="plain" data-title="plain" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code><span class="line"><span>| 31-8 bits (24位) | 7-4 bits (4位) | 3-0 bits (4位) |</span></span>\n<span class="line"><span>| -----------------| -------------- | -------------- |</span></span>\n<span class="line"><span>| 行锁类型          | 锁级别          | 锁模式          |</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ol><li><p><strong>锁模式</strong>（Lock Mode）：低4位</p><p>存储内容：表示锁的基本操作类型。</p><p>取值范围：</p><ul><li><p><code>0x0</code> (LOCK_IS)：意向共享锁（表级）。</p></li><li><p><code>0x1</code> (LOCK_IX)：意向排他锁（表级）。</p></li><li><p><code>0x2</code> (LOCK_S)：行级共享锁。</p></li><li><p><code>0x3</code> (LOCK_X)：行级排他锁。</p></li><li><p><code>0x4</code> (LOCK_AUTO_INC)：自增锁。</p></li></ul><p>作用：决定锁的读写权限和兼容性。</p></li><li><p><strong>锁级别</strong>（Lock Level）：5-8位（4位）</p><p>存储内容：标识锁的粒度级别。</p><p>取值：</p><ul><li><code>0x10</code> (LOCK_TABLE)：表级锁。</li><li><code>0x20</code> (LOCK_REC)：行级锁。</li></ul><p>作用：区分锁是作用在表还是行。</p></li><li><p><strong>行锁类型</strong>（Record Lock Type）：高位24位</p><p>存储内容：行锁的具体类型。</p><p>取值：</p><ul><li><p><code>0x10000000</code> (LOCK_WAIT)：等待状态。</p></li><li><p><code>0x20000000</code> (LOCK_REC_NOT_GAP)：记录锁（非间隙）。</p></li><li><p><code>0x40000000</code> (LOCK_GAP)：间隙锁。</p></li><li><p><code>0x80000000</code> (LOCK_ORDINARY)：临键锁（Next-Key Lock）。</p></li></ul><p>作用：控制行锁的范围（如间隙、记录本身或组合）。</p></li></ol><h4 id="_1-5-2-记录锁-record-lock" tabindex="-1"><a class="header-anchor" href="#_1-5-2-记录锁-record-lock"><span>1.5.2 记录锁（Record Lock）</span></a></h4><figure><img src="'+c+'" alt="1755422949483" tabindex="0" loading="lazy"><figcaption>1755422949483</figcaption></figure><ul><li>锁定索引记录本身。</li><li>总是锁定<strong>索引项</strong>（即使表没有显式索引，InnoDB 也会创建一个隐藏的聚簇索引）。</li><li><code>SELECT ... FOR UPDATE</code>, <code>UPDATE</code>, <code>DELETE</code> 在精确匹配到唯一索引（包括主键）的等值查询时，通常加记录锁（X 模式）。</li><li>例如：<code>DELETE FROM users WHERE id = 10;</code> (id 是主键) -&gt; 对 id=10 的记录加 X 记录锁。</li></ul><p>行锁都是锁在索引上的，如果要锁的列没有索引，会进行全表记录加锁。如果锁的是二级索引，那么一般情况下都是<strong>先锁二级索引再锁聚簇索引</strong></p><p>针对唯一索引进行检索时，对已存在的记录进行等值匹配时，将会自动优化为行锁。 InnoDB的行锁是针对于索引加的锁，不通过索引条件检索数据，那么InnoDB将对表中的所有记录加锁，此时 就会<strong>升级为表锁</strong>。</p><h4 id="_1-5-3-间隙锁-gap-lock" tabindex="-1"><a class="header-anchor" href="#_1-5-3-间隙锁-gap-lock"><span>1.5.3 间隙锁（Gap Lock）</span></a></h4><p>锁定<strong>索引记录之间的间隙</strong>，或者第一个索引记录之前或最后一个索引记录之后的“间隙”。</p><p><strong>目的</strong>：<strong>防止幻读</strong>（Phantom Read），确保在同一个事务内多次执行相同的范围查询会得到一致的结果集（阻止其他事务在间隙中插入新记录）。</p><p><strong>仅存在于 REPEATABLE READ (RR) 隔离级别</strong>。<code>READ COMMITTED</code> (RC) 级别下通常<strong>不</strong>使用间隙锁。</p><p>间隙锁之间<strong>不冲突</strong>。不同事务可以在同一个间隙上加间隙锁（因为它们都是为了防止插入，而阻止插入这个动作本身不冲突）。</p><p><strong>举例</strong>：<code>SELECT * FROM users WHERE age BETWEEN 20 AND 30 FOR UPDATE;</code> (RR 级别) -&gt; 除了锁定 age 在 20-30 之间的所有记录（X 记录锁），还会锁定 (min_age, 20) 和 (30, max_age) 这两个间隙（X 间隙锁），阻止其他事务插入 age=19, 21, 29, 31 等在这个范围内的新记录。</p><p>锁定索引记录间隙（不含该记录），确保索引记录间隙不变，防止其他事务在这个间隙进行insert，产生幻读。在RR隔离级别下都支持。</p><figure><img src="'+h+'" alt="1755422949483" tabindex="0" loading="lazy"><figcaption>1755422949483</figcaption></figure><p>举例：假如说现在要更新 id = 13的数据 那么就会对12 -- 16 之间的间隙加锁。只锁间隙不锁两边数据 也是是开区间</p><h4 id="_1-5-4-临键锁" tabindex="-1"><a class="header-anchor" href="#_1-5-4-临键锁"><span>1.5.4 临键锁</span></a></h4><p>默认情况下，InnoDB在 REPEATABLE READ事务隔离级别运行，InnoDB使用 next-key 锁进行搜索和索引扫描，以防止幻读。</p><figure><img src="'+u+'" alt="1755422949483" tabindex="0" loading="lazy"><figcaption>1755422949483</figcaption></figure><p>临建锁（next-key）= <strong>记录锁 + 间隙锁</strong>，它既能保护该条记录，又能阻止别的事务 将新记录插入被保护记录前边的 间隙 。临键锁是innodb的最基本的锁单位，只是在某些情况下会退化成记录锁或者间隙锁。</p><p>在加锁的时候，我们都是对记录加锁，如果是间隙锁，我们也是对开区间的最后一条记录上加上一把锁。所以我们在插入记录时，我们也需要判断这个插入区间往后的第一条记录上的锁情况。</p><p>锁定一个<strong>索引记录</strong>以及<strong>该记录之前的间隙</strong>。即锁定一个<strong>左开右闭</strong>的区间 <code>(previous_gap, current_record]</code>。</p><p><strong>目的</strong>：结合记录锁和间隙锁的功能，<strong>既防止当前记录被修改/删除，也防止在记录前的间隙中插入新记录（防止幻读）</strong>。</p><h4 id="_1-5-5-插入意向锁" tabindex="-1"><a class="header-anchor" href="#_1-5-5-插入意向锁"><span>1.5.5 插入意向锁</span></a></h4><p>插入意向锁本质其实就只是个gap锁，用来解决当前读的幻读问题。插入意向锁不会阻塞其他行锁，但是gap行锁无论S还是X都会阻塞插入意向锁。</p><p><strong>目的</strong>： 表示一个事务<strong>打算</strong>在某个间隙中<strong>插入</strong>新记录。它<strong>不会阻塞</strong>其他事务在<strong>同一个间隙</strong>上加间隙锁（因为间隙锁本身就是为了防止插入，允许多个事务“声明”要防止插入是合理的），但会<strong>与已有的间隙锁/临键锁冲突</strong>（即如果已经有事务在该间隙上加了间隙锁/临键锁来防止插入，那么插入意向锁会等待）。</p><p><strong>作用</strong>： 提高并发插入性能。多个事务只要不是插入到<strong>完全相同的位置</strong>（相同的间隙和相同的插入值可能导致唯一键冲突），即使它们要在同一个间隙插入，只要没有其他事务持有该间隙的锁阻止插入，它们都可以获得插入意向锁并执行插入。它<strong>通知</strong>其他事务“我想在这里插个值，如果你们没意见（没有阻塞我的锁）我就插了”。</p><p>例如：事务 A 持有间隙锁锁定 (5, 10)。事务 B 想插入 id=7。事务 B 在插入前会尝试获取 (5, 10) 间隙上的<strong>插入意向锁</strong>。因为事务 A 已经在该间隙上持有（排他）间隙锁，事务 B 的插入意向锁请求会<strong>阻塞</strong>，直到事务 A 释放间隙锁。</p><h2 id="_2-加锁原则" tabindex="-1"><a class="header-anchor" href="#_2-加锁原则"><span>2. 加锁原则</span></a></h2><ol><li><p><strong>索引上的等值查询(唯一索引)，给不存在的记录加锁时, 优化为间隙锁 。</strong></p></li><li><p><strong>索引上的等值查询(非唯一普通索引)，向右遍历时最后一个值不满足查询需求时，next-key lock 退化为间隙锁。</strong></p></li><li><p><strong>索引上的范围查询(唯一索引) 会访问到不满足条件的第一个值为止。</strong></p></li><li><p><strong>查找过程中访问到的对象才会加锁</strong></p></li></ol><blockquote><p>注意：间隙锁唯一目的是防止其他事务插入间隙。间隙锁可以共存，一个事务采用的间隙锁不会</p><p>阻止另一个事务在同一间隙上采用间隙锁。</p></blockquote><p>例：</p><table><thead><tr><th>id</th><th>age</th></tr></thead><tbody><tr><td>1</td><td>3</td></tr><tr><td>3</td><td>5</td></tr><tr><td>4</td><td>5</td></tr><tr><td>7</td><td>9</td></tr></tbody></table><ol><li>执行sql <code>update user set age = 10 where id = 5 </code> 锁是如何加的呢 <ol><li>首先默认临键锁会对（4 , 7]进行加锁，</li><li>然后根据原则1 会优化成间隙锁 最终加锁范围id（4,7）</li></ol></li><li>执行sql <code>select * from user where age = 5</code> 锁是如何加的呢 <ol><li>首先会索引会定位到第一条age为5的数据进行临键锁加锁 (3 , 5]</li><li>然后继续向右遍历 找到第二个5 继续向右遍历找到 9 加锁 (5 , 9]</li><li>根据原则2 对最后一个值9 不满足查询条件 退化为间隙锁 所以最终加速范围（3 ，9）</li></ol></li><li>执行sql <code>select * from user where age &gt; 5</code> 锁是如何加的呢 <ol><li>首先找到5以后往右遍历 进行加锁 （5 , 9]</li><li>根据原则3 找到最后一个不满足条件的 但是最后是9 再往后没有的，这时候会加一个到正无穷的锁也就是说最终加锁（5 ，+无穷）</li></ol></li><li>执行sql <code>select age from user where age = 5</code> 的过程中 执行 <code>update user set age = 10 where id = 5</code><ol><li>首先和上面分析的一样 会把二级索引的区间（3 ，9）锁住，但是更新语句仍然会成功</li><li>这是为啥 根据原则4 因为覆盖索引不需要回表 那么主键索引没有锁 仍然可以更新</li></ol></li></ol><h2 id="_3-事务" tabindex="-1"><a class="header-anchor" href="#_3-事务"><span>3. 事务</span></a></h2><h3 id="_3-1-事务的四大特性" tabindex="-1"><a class="header-anchor" href="#_3-1-事务的四大特性"><span>3.1 事务的四大特性</span></a></h3><ul><li>原子性(Atomicity)：事务是数据库的逻辑工作单位，事务中包含的各操作要么都做，要么都不做。 undolog来保证</li><li>一致性(Consistency)：事务开始前和结束后，数据库的完整性约束没有被破坏 。比如A向B转账，不可能A扣了钱，B却没收到。</li><li>隔离型(Isolation)：一个事务的执行不能被其它事务干扰。即一个事务内部的操作及使用的数据对其它并发事务是隔离的，并发执行的各个事务之间不能互相干扰。mvcc和锁来保证</li><li>持久性(Durability)：指一个事务一旦提交，它对数据库中的数据的改变就应该是永久性的。接下来的其它操作或故障不应该对其执行结果有任何影响。redolog 来保证</li></ul><h3 id="_3-2-事务的隔离级别" tabindex="-1"><a class="header-anchor" href="#_3-2-事务的隔离级别"><span>3.2 事务的隔离级别</span></a></h3><p>首先介绍几个概念：</p><ol><li><p>脏读：事务A读取了事务B更新的数据，然后B回滚操作，那么A读取到的数据是脏数据</p></li><li><p>不可重复读：事务 A 多次读取同一数据，事务 B 在事务A多次读取的过程中，对数据作了更新并提交，导致事务A多次读取同一数据时，结果 不一致。</p></li><li><p>幻读：系统管理员A将数据库中所有学生的成绩从具体分数改为ABCDE等级，但是系统管理员B就在这个时候插入了一条具体分数的记录，当系统管理员A改结束后发现还有一条记录没有改过来，就好像发生了幻觉一样，这就叫幻读。</p><p>小结：不可重复读的和幻读很容易混淆，不可重复读侧重于修改，幻读侧重于新增或删除。</p></li></ol><table><thead><tr><th>隔离级别</th><th>脏读 (Dirty Read)</th><th>不可重复读 (Non-Repeatable Read)</th><th>幻读 (Phantom Read)</th></tr></thead><tbody><tr><td><code>READ UNCOMMITTED</code> (读未提交)</td><td>可能</td><td>可能</td><td>可能</td></tr><tr><td><code>READ COMMITTED</code> (读已提交)</td><td><strong>不可能</strong></td><td>可能</td><td>可能</td></tr><tr><td><code>REPEATABLE READ</code> (可重复读)</td><td><strong>不可能</strong></td><td><strong>不可能</strong></td><td>可能 (InnoDB <strong>不可能</strong>)</td></tr><tr><td><code>SERIALIZABLE</code> (串行化)</td><td><strong>不可能</strong></td><td><strong>不可能</strong></td><td><strong>不可能</strong></td></tr></tbody></table><h3 id="_3-3-mvcc" tabindex="-1"><a class="header-anchor" href="#_3-3-mvcc"><span>3.3 MVCC</span></a></h3><p>Multi Version Concurrency Control用来实现不加锁情况下的读一致性和隔离性。</p><p>MVCC的实现，基于<strong>undolog</strong>、<strong>版本链</strong>、<strong>readview</strong>。</p><p><strong>作用</strong>：减少锁竞争，提高并发性能，尤其适用于读多写少场景</p><figure><img src="'+_+'" alt="1755422949483" tabindex="0" loading="lazy"><figcaption>1755422949483</figcaption></figure><p><strong>工作流程 (读操作)：</strong></p><ol><li>当开启一个读事务（如 SELECT）时（根据隔离级别决定创建 Read View 的时机）。</li><li>访问数据行时，获取该行当前的 <code>DB_TRX_ID（最近修改事务ID）</code>。</li><li>根据当前事务的 <strong>Read View</strong> 和 <strong>可见性判断规则</strong>，判断该 <code>DB_TRX_ID</code> 对应的版本对当前事务是否可见。</li><li>如果不可见，则通过该行的 <code>DB_ROLL_PTR</code> 找到 Undo Log 中的<strong>历史版本</strong>。</li><li>重复步骤 3 和 4，沿着<strong>版本链</strong>向上查找，直到找到满足可见性规则的版本（或到达链尾）。</li><li>返回找到的可见版本的数据。</li></ol><p><strong>工作流程 (写操作 - UPDATE/DELETE)：</strong></p><ul><li>写操作（增删改）总是基于<strong>最新的已提交版本</strong>进行。</li><li><code>UPDATE</code>： <ol><li>定位要更新的行（使用当前读，可能加锁）。</li><li>将当前行的内容（最新已提交版本）拷贝到 Undo Log，形成旧版本，并设置 <code>DB_ROLL_PTR</code> 指向它。</li><li>更新当前行的数据，设置 <code>DB_TRX_ID</code> 为当前事务 ID。</li><li>将 <code>DB_ROLL_PTR</code> 指向刚刚创建的 Undo Log 记录。</li></ol></li><li><code>DELETE</code>： 类似于 UPDATE，逻辑上标记为删除（设置 <code>DB_TRX_ID</code> 和 <code>DB_ROLL_PTR</code>），物理删除由 Purge 线程完成。</li><li><code>INSERT</code>： 新插入的行，其 <code>DB_TRX_ID</code> 为当前事务 ID，<code>DB_ROLL_PTR</code> 指向一个特殊的 Undo Log 记录（表示空）。</li></ul><h4 id="_3-3-1-undolog" tabindex="-1"><a class="header-anchor" href="#_3-3-1-undolog"><span>3.3.1 undolog</span></a></h4><p>回滚日志，在insert、update、delete的时候产生的便于数据回滚的日志。</p><p>当insert的时候，产生的undo log日志只在回滚时需要，在事务提交后，可被立即删除。</p><p>而update、delete的时候，产生的undo log日志不仅在回滚时需要，在快照读时也需要，<strong>不会立即被删除</strong>。</p><h4 id="_3-3-2-版本链" tabindex="-1"><a class="header-anchor" href="#_3-3-2-版本链"><span>3.3.2 版本链</span></a></h4><p>在数据存储时除了正常要存入的数据还会存入三个隐藏字段：</p><table><thead><tr><th>隐藏字段</th><th>含义</th></tr></thead><tbody><tr><td>DB_TRX_ID</td><td>最近修改事务ID，记录插入这条记录或最后一次修改该记录的事务ID。</td></tr><tr><td>DB_ROLL_PTR</td><td>回滚指针，指向这条记录的上一个版本，用于配合undo log，指向上一个版本。</td></tr><tr><td>DB_ROW_ID</td><td>隐藏主键,如果表结构没有指定主键,将会生成该隐藏字段。</td></tr></tbody></table><p>实际还有一个删除flag隐藏字段, 既记录被更新或删除并不代表真的删除，而是删除flag变了（假删除）</p><figure><img src="'+E+'" alt="1755422949483" tabindex="0" loading="lazy"><figcaption>1755422949483</figcaption></figure><p>版本链是如何工作的呢：</p><p>每次更新操作，都会在 Undo Log 中生成一条记录，记录旧数据，新数据的 <code>DB_ROLL_PTR</code> 指向旧数据，再次更新新数据再指向旧数据，形成一条<strong>单向链表</strong>，即版本链。链头是最新版本，链尾是最旧版本。</p><h4 id="_3-3-3-readview" tabindex="-1"><a class="header-anchor" href="#_3-3-3-readview"><span>3.3.3 readview</span></a></h4><p>ReadView（读视图）是 快照读 SQL执行时MVCC提取数据的依据，记录并维护系统当前活跃的事务（未提交的）id。</p><p>生成时机：</p><ul><li><strong>RR</strong>（可重复读）隔离级别下：<strong>事务在启动时</strong>会生成一个 Read View，这个 Read View 会在整个事务过程中保持不变，所以事务期间看到的数据都是一致的，可重复读嘛。</li><li><strong>RC</strong>（读已提交）隔离级别中：<strong>每次执行查询操作</strong>时都会生成新的 Read View，这样就能读到其他事务提交的最新数据啦。</li></ul><p><strong>关键内容 (快照信息)：</strong></p><ul><li><code>m_ids</code>： 创建 Read View 时，系统中<strong>活跃</strong>（未提交）的事务 ID 列表。</li><li><code>min_trx_id</code>： <code>m_ids</code> 中的最小值。</li><li><code>max_trx_id</code>： 创建 Read View 时，系统应分配给<strong>下一个事务</strong>的 ID 值。</li><li><code>creator_trx_id</code>： 创建该 Read View 的事务自身的 ID（对于只读事务，默认为 0）。</li></ul><p><strong>可见性判断规则：</strong> 当访问一条记录时，检查该记录的 <strong>DB_TRX_ID</strong> (创建/修改它的事务ID)：</p><ol><li><p>如果 <code>DB_TRX_ID &lt; min_trx_id</code>： 说明该记录版本在 Read View 创建<strong>之前</strong>就已提交，<strong>可见</strong>。</p></li><li><p>如果 <code>DB_TRX_ID &gt;= max_trx_id</code>： 说明该记录版本在 Read View 创建<strong>之后</strong>才被修改（由将来事务创建），<strong>不可见</strong>。</p></li><li><p>如果 <code>min_trx_id &lt;= DB_TRX_ID &lt; max_trx_id</code>：</p><ul><li>如果 <code>DB_TRX_ID</code> <strong>在</strong> <code>m_ids</code> 列表中： 说明创建该版本的事务在创建 Read View 时<strong>仍活跃</strong>（未提交），该版本<strong>不可见</strong>。</li><li>如果 <code>DB_TRX_ID</code> <strong>不在</strong> <code>m_ids</code> 列表中： 说明创建该版本的事务在创建 Read View 时<strong>已提交</strong>，该版本<strong>可见</strong>。</li></ul></li><li><p>如果 <code>DB_TRX_ID == creator_trx_id</code>： 该记录版本是当前事务自己修改的，<strong>可见</strong>。</p></li><li><p><strong>不可见怎么办？</strong> 沿着记录的 <strong>DB_ROLL_PTR</strong> 指针，在 <strong>Undo Log</strong> 中找到该记录的<strong>历史版本</strong>，重新应用上述规则判断，直到找到满足条件的可见版本。</p></li></ol>',181),L={},m=(0,l(66262).A)(L,[["render",function(i,t){return(0,n.uX)(),(0,n.CE)("div",null,[k,b,(0,n.Q3)(" more "),A])}]]),D=JSON.parse('{"path":"/program/mysql/mysql%E8%BF%9B%E9%98%B6.html","title":"Mysql进阶","lang":"zh-CN","frontmatter":{"totail":null,"icon":"book","date":"2024-10-01T00:00:00.000Z","category":["编程"],"tag":["Mysql"],"description":"索引与算法、事务与锁...","head":[["meta",{"property":"og:url","content":"https://github.com/DUDU-dudu-DUDU/xwblog/xwblog/program/mysql/mysql%E8%BF%9B%E9%98%B6.html"}],["meta",{"property":"og:site_name","content":"小吴 Blog"}],["meta",{"property":"og:title","content":"Mysql进阶"}],["meta",{"property":"og:description","content":"索引与算法、事务与锁..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:locale","content":"zh-CN"}],["meta",{"property":"og:updated_time","content":"2025-08-31T03:13:03.000Z"}],["meta",{"property":"article:author","content":"xiaowu"}],["meta",{"property":"article:tag","content":"Mysql"}],["meta",{"property":"article:published_time","content":"2024-10-01T00:00:00.000Z"}],["meta",{"property":"article:modified_time","content":"2025-08-31T03:13:03.000Z"}],["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"Mysql进阶\\",\\"image\\":[\\"\\"],\\"datePublished\\":\\"2024-10-01T00:00:00.000Z\\",\\"dateModified\\":\\"2025-08-31T03:13:03.000Z\\",\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"xiaowu\\",\\"url\\":\\"https://dudu-dudu-dudu.github.io/xwblog/zh/\\"}]}"]]},"headers":[{"level":2,"title":"1. 索引结构","slug":"_1-索引结构","link":"#_1-索引结构","children":[{"level":3,"title":"1.1 B树与B+树","slug":"_1-1-b树与b-树","link":"#_1-1-b树与b-树","children":[]}]},{"level":2,"title":"2. 索引的分类","slug":"_2-索引的分类","link":"#_2-索引的分类","children":[]},{"level":2,"title":"3. 索引的特征","slug":"_3-索引的特征","link":"#_3-索引的特征","children":[{"level":3,"title":"3.1 回表","slug":"_3-1-回表","link":"#_3-1-回表","children":[]},{"level":3,"title":"3.2 索引覆盖","slug":"_3-2-索引覆盖","link":"#_3-2-索引覆盖","children":[]},{"level":3,"title":"3.3 索引下推","slug":"_3-3-索引下推","link":"#_3-3-索引下推","children":[]},{"level":3,"title":"3.4 最左匹配原则","slug":"_3-4-最左匹配原则","link":"#_3-4-最左匹配原则","children":[]}]},{"level":2,"title":"4. 索引的失效场景","slug":"_4-索引的失效场景","link":"#_4-索引的失效场景","children":[]},{"level":2,"title":"5. 索引的设计与优化","slug":"_5-索引的设计与优化","link":"#_5-索引的设计与优化","children":[{"level":3,"title":"5.1 索引的创建","slug":"_5-1-索引的创建","link":"#_5-1-索引的创建","children":[]},{"level":3,"title":"5.2 SQL优化","slug":"_5-2-sql优化","link":"#_5-2-sql优化","children":[]}]},{"level":2,"title":"1. 锁的分类","slug":"_1-锁的分类","link":"#_1-锁的分类","children":[{"level":3,"title":"1.1 共享锁和排他锁","slug":"_1-1-共享锁和排他锁","link":"#_1-1-共享锁和排他锁","children":[]},{"level":3,"title":"1.2 全局锁","slug":"_1-2-全局锁","link":"#_1-2-全局锁","children":[]},{"level":3,"title":"1.3 元数据锁（DML）","slug":"_1-3-元数据锁-dml","link":"#_1-3-元数据锁-dml","children":[]},{"level":3,"title":"1.4 表锁","slug":"_1-4-表锁","link":"#_1-4-表锁","children":[]},{"level":3,"title":"1.5 行锁","slug":"_1-5-行锁","link":"#_1-5-行锁","children":[]}]},{"level":2,"title":"2. 加锁原则","slug":"_2-加锁原则","link":"#_2-加锁原则","children":[]},{"level":2,"title":"3. 事务","slug":"_3-事务","link":"#_3-事务","children":[{"level":3,"title":"3.1 事务的四大特性","slug":"_3-1-事务的四大特性","link":"#_3-1-事务的四大特性","children":[]},{"level":3,"title":"3.2 事务的隔离级别","slug":"_3-2-事务的隔离级别","link":"#_3-2-事务的隔离级别","children":[]},{"level":3,"title":"3.3 MVCC","slug":"_3-3-mvcc","link":"#_3-3-mvcc","children":[]}]}],"git":{"createdTime":1756609983000,"updatedTime":1756609983000,"contributors":[{"name":"DUDU","email":"930824238@qq.com","commits":1}]},"readingTime":{"minutes":36.96,"words":11087},"filePathRelative":"program/mysql/mysql进阶.md","localizedDate":"2024年10月1日","excerpt":"\\n<p>索引与算法、事务与锁...</p>\\n","autoDesc":true}')}}]);